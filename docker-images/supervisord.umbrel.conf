# ==============================================================================
# Garbageman Nodes Manager - Supervisord Configuration (Umbrel)
# ==============================================================================
# Manages three services within the Umbrel container:
# 1. supervisor - Multi-daemon manager (manages Bitcoin node instances)
# 2. api        - Fastify REST API server (backend)
# 3. ui         - Next.js web interface (frontend)
#
# All services:
# - Run as user 1000 (Umbrel standard non-root user)
# - Auto-restart on failure (up to 3 retries)
# - Log to stdout/stderr for Umbrel log collection
# - Receive environment variables from docker-entrypoint.sh
#
# Environment variables are interpolated using %(ENV_VAR_NAME)s syntax
# ==============================================================================

[supervisord]
nodaemon=true
logfile=/var/log/supervisor/supervisord.log
logfile_maxbytes=50MB
logfile_backups=10
pidfile=/var/run/supervisor/supervisord.pid
loglevel=info
# Don't set user here - let supervisord run as root, individual programs set user=1000

# ==============================================================================
# Multi-Daemon Supervisor
# ==============================================================================
[program:supervisor]
command=node /app/supervisor/dist/supervisor.stub.js
directory=/app/supervisor
autostart=true
autorestart=true
startretries=3
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
user=1000
environment=
    SUPERVISOR_PORT=%(ENV_SUPERVISOR_PORT)s,
    DATA_DIR=%(ENV_DATA_DIR)s,
    ARTIFACTS_DIR=%(ENV_ARTIFACTS_DIR)s,
    ENVFILES_DIR=%(ENV_ENVFILES_DIR)s

# ==============================================================================
# API Server (Fastify)
# ==============================================================================
[program:api]
command=node /app/api/dist/server.js
directory=/app/api
autostart=true
autorestart=true
startretries=3
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
user=1000
environment=
    PORT=%(ENV_API_PORT)s,
    HOST=0.0.0.0,
    ENVFILES_DIR=%(ENV_ENVFILES_DIR)s,
    SUPERVISOR_URL=http://127.0.0.1:%(ENV_SUPERVISOR_PORT)s,
    LOG_LEVEL=%(ENV_LOG_LEVEL)s,
    NODE_ENV=%(ENV_NODE_ENV)s,
    TOR_PROXY_HOST=%(ENV_TOR_PROXY_HOST)s,
    TOR_PROXY_PORT=%(ENV_TOR_PROXY_PORT)s,
    ARTIFACTS_DIR=%(ENV_ARTIFACTS_DIR)s,
    DATA_DIR=%(ENV_DATA_DIR)s,
    WRAPPER_UI_PASSWORD=%(ENV_WRAPPER_UI_PASSWORD)s,
    WEBUI_PASSWORD=%(ENV_WEBUI_PASSWORD)s

# ==============================================================================
# UI Server (Next.js)
# ==============================================================================
[program:ui]
command=node /app/ui/server.js
directory=/app/ui
autostart=true
autorestart=true
startretries=3
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
user=1000
environment=
    PORT=%(ENV_UI_PORT)s,
    HOSTNAME=0.0.0.0,
    NEXT_PUBLIC_API_BASE=http://127.0.0.1:%(ENV_API_PORT)s,
    NODE_ENV=%(ENV_NODE_ENV)s
